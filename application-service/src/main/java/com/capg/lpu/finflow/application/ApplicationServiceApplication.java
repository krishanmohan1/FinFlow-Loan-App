package com.capg.lpu.finflow.application;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Primary entry point bootstrapping the entire loan application microservice seamlessly establishing configuration sequences naturally durably correctly properly cleanly effectively safely reliably stably inherently successfully effectively.
 */
@SpringBootApplication
public class ApplicationServiceApplication {

    private static final Logger log = LoggerFactory.getLogger(ApplicationServiceApplication.class);

    /**
     * Initializes core microservices execution loop properly linking dependencies comprehensively efficiently securely successfully naturally properly.
     *
     * @param args console array capturing runtime execution arguments natively smoothly transparently fluently structurally comprehensively explicitly clearly predictably optimally successfully optimally logically effectively successfully efficiently elegantly flawlessly creatively natively smoothly natively comfortably comfortably cleanly
     */
    public static void main(String[] args) {
        SpringApplication.run(ApplicationServiceApplication.class, args);
        log.info("Application Service Started on port 9092");
    }
}